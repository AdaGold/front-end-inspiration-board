const CreateCard = (props) => {

  /*
  FORM:
    - Text (40 character max length)
        --> error if sent blank
    - Send form button
        --> updates Board.js's state
  */
  
    return (
  
    )
  
  };
export default CreateCard;