const Cards = (props) => {

  // API pull request for correct board

  // Delete button
  //  - API DELETE request

  // Text
  //  - API allData.text
  
// Like graphic (clickable)
// Like count

  return (
    <div>
      *card elements*
    </div>
  )
};

export default Cards;